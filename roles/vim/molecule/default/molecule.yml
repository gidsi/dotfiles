---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: archlinux-python:latest
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
