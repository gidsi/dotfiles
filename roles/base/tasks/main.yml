---
- name: Generate SSH keys
  shell: "ssh-keygen -b 4096 -f ~/.ssh/id_rsa"
  args:
    creates: "~/.ssh/id_rsa"

- name: Install packages
  pacman: name={{ item }} state=latest
  become: true
  with_items:
    - cloc
    - base-devel
    - the_silver_searcher
    - keychain
    - whois
    - htop
    - dnsutils
