- name: Collecting Facts
  hosts: zero scraps betelgeuse hengst
  gather_facts: yes

- name: Set up local environment
  hosts: zero scraps betelgeuse hengst
  vars_files:
      - 'vars/vault.yml'
      - 'vars/vars.yml'
      - 'vars/git_private.yml'
  roles:
    - git
    - {role: package_manager, tags: ['bootstrap']}
    - zsh
    - i3
    - vim
    - ag
    - mutt
    - cloc
    - irssi
