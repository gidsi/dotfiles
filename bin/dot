echo "Updating local dev environment..."
cd ~/.dotfiles
git stash
git pull origin master
git stash pop
ansible-playbook -l $(hostname) -i ~/.dotfiles/desktops ~/.dotfiles/local_env.yml --skip-tags "bootstrap" -K $@
